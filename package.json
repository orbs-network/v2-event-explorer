{
  "name": "poslogs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd backend && npm install && cd ../frontend && npm install",
    "start-backend": "cd backend && npm run start-backend",
    "start-frontend": "cd frontend && npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@orbs-network/orbs-ethereum-contracts-v2": "0.0.31",
    "body-parser": "^1.19.0"
  }
}
